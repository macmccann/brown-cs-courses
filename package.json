{
  "name": "development",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "gh-pages": "^1.0.0",
    "gh-pages-deploy": "^0.4.2",
    "react": "^16.1.0",
    "react-dom": "^16.1.0",
    "react-scripts": "0.9.5"
  },
  "devDependencies": {
    "gh-pages-deploy": "^0.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "homepage": "https://macmccann.github.io/brown-cs-courses"
}
